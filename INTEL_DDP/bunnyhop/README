# Taken + modified from https://github.com/0xADE1A1DE/BunnyHop/tree/main 

Note when installing AssemblyLine, their instruction don't quite work, had to use `./configure --without-bash-completion-dir && make  && sudo make install` install to install to disable autocompleation which caused issues with the configure call 

Note: ignore assembly line, we don't use it anymore 